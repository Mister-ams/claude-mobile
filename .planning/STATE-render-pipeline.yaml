schema_version: 1
feature: render-pipeline
plan: .planning/plan-render-pipeline.md
tech_design: .planning/tech-design-render-pipeline.md
status: active
updated: 2026-05-02

# Renamed 2026-05-01 from v4-thin-viewer.
# Old shape (tmux capture-pane -> ANSI-to-HTML poll) was incompatible with the
# Claude Code CLI's full-screen TUI model (alt-screen + DECSTBM + mouse + OSC 8).
# New shape: headless xterm on server + cell-grid + row-diff transport.
# Versioning: patch-only on the 3.2.x line (no v4 jump, no minor bump).

sprint:
  current: 1
  total: 1

wave:
  current: 6
  total: 7
  # W6 default-flip attempted 2026-05-02 (commit 3c1c402) and reverted same
  # day (commit da4b2fc). History/scrollback did not render in grid mode
  # when default; works fine under ?renderer=grid opt-in. Investigation
  # pointers in memory/project_w6_history_render_bug.md. T26 (legacy WS
  # handler removal) and T27 (xterm.js vendor delete) BLOCKED until fix.

completed:
  - wave: W1
    release: 3.2.2
    shipped: 2026-05-01
    commits: [1519dc4, ecb020d, b703128, 01394f6]
    note: "Server-side onData coalescer + client rAF batching. User-validated as 'huge improvement'. CI green on master."
  - wave: W2
    release: 3.2.3
    shipped: 2026-05-01
    commits: [213059a, 9a07555, c03454d]
    note: "WS-path fidelity audit (zero filtering confirmed). Resize wired to dtach PTY with TDD-aligned bounds [10,400]/[5,200]."
  - wave: W3
    release: 3.2.4
    shipped: 2026-05-02
    commits: [82c10ab, bc36043]
    note: "Shared rAF scheduler (scheduleOnce + requestLive/dropLive). Migrated T03 batcher and scrollBottom to it. requestLive/dropLive reserved for W7 rope-tail."
  - wave: W4
    release: 3.2.5
    shipped: 2026-05-02
    commits: [c3d32da, feba3f1]
    note: "@xterm/headless v6.0.0 mirror + spike. 13/13 scripted checks pass; 2 workarounds documented (async writes, OSC 8 via registerOscHandler). Verdict GO for W5."
  - wave: W5
    release: 3.2.6
    shipped: 2026-05-02
    commits: [9c1aaa4, eb44141, 16f1e4a, b97e2cf, 3709e06]
    note: "Cell-grid + row-RLE diff transport behind per-session flag (?renderer=grid). Server: buildSnapshot/buildFrame, OSC 8 URI registry via registerOscHandler. Client: gridTerms{}, makeGridTerm, Snapshot/Frame handlers, OSC 8 anchors, E8 tab-return resync. Default xterm.js path unchanged."
    deferred:
      - task: T22
        title: mouse-tracking event forwarding
        moved_to: W7
        reason: "Needs server-side CSI mode tracking + client encoder + listener mgmt (~150 LOC); risks breaking grid-mode scroll/tap UX. Not commonly used in Claude Code REPL. Per-session flag default-off contains the impact."
    known_limits:
      - "Cursor tracked but not visually rendered (W7)."
      - "Scrollback display capped at 1000 rows; server keeps full 5000."
      - "ANSI palette 16..255 falls through to default (W7)."
      - "Theme switching (light/dark) doesn't repaint already-rendered rows; refresh refreshes from next snapshot."

blocked:
  - wave: W6
    task: T24
    attempted: 2026-05-02
    reverted: 2026-05-02
    flip_commit: 3c1c402
    revert_commit: da4b2fc
    symptom: "history/scrollback does not render in grid mode when default-on; opt-in (?renderer=grid) works fine"
    investigation_pointers: memory/project_w6_history_render_bug.md
    blocks: [T26, T27, T28, W7]

retrospective: []
